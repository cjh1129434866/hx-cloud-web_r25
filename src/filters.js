import { $utils } from '@helper'

export default {
  dateConvert(time) {
    return $utils.dateConvert(time)
  },
  dayConvert(time) {
    return $utils.dayConvert(time)
  },
  timeConvert(time) {
    return $utils.timeConvert(time)
  }
}
